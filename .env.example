# Database Configuration
DB_HOST=db
DB_PORT=3306
DB_NAME=joki_balqis_library_app
DB_USER=balqis_user
DB_PASSWORD=balqis_password
DB_ROOT_PASSWORD=rootpassword

# Node.js Environment
NODE_ENV=production

# Backend Configuration
BACKEND_PORT=3000

# Frontend Configuration
FRONTEND_PORT=80

# JWT Secret Key (Change this to a secure random string!)
JWT_SECRET=your_jwt_secret_key_change_this_to_something_long_and_random

# SMTP Email Configuration
SMTP_HOST=smtp.zeptomail.com
SMTP_PORT=587
SMTP_USER=your_email@example.com
SMTP_PASSWORD=your_email_password
SMTP_FROM=noreply@perpustakaan.ac.id

# API Configuration (used by frontend)
# Note: Update this based on your deployment domain
VITE_API_BASE_URL=http://localhost:3000/api
